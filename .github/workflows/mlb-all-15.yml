name: Deprecated - MLB all-15 uncapped rule

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Obsolete migration guard
        run: |
          echo "This workflow is intentionally disabled."
          echo "Current MLB EZPZ policy is a maximum of 2 V2 picks per date, each requiring a 15%+ V2 Market Gap."
          echo "Do not reapply the former uncapped all-15 migration."
