name: Validate MLB RLM-only one-pick policy

on:
  push:
    branches: [main]
    paths:
      - 'app/api/public-data-v2/route.ts'
      - 'lib/mlbTrendV2.ts'
      - 'app/page.tsx'
      - '.github/workflows/mlb-all-15.yml'
  pull_request:
    paths:
      - 'app/api/public-data-v2/route.ts'
      - 'lib/mlbTrendV2.ts'
      - 'app/page.tsx'
      - '.github/workflows/mlb-all-15.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mlb-rlm-only-one-pick-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund
      - run: npm run build
