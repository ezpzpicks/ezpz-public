import fs from "node:fs";

const path = "lib/footballWeeklyMarket.ts";
let text = fs.readFileSync(path, "utf8");

const marker = "type CanonicalFootballGame = {";
if (!text.includes(marker)) {
  const anchor = "async function loadPostedSplits(sport: FootballSport) {";
  const helper = `type CanonicalFootballGame = {\n  date: string;\n  awayAliases: string[];\n  homeAliases: string[];\n};\n\ntype EspnFootballTeam = {\n  abbreviation?: string; displayName?: string; shortDisplayName?: string; location?: string; name?: string;\n};\n\ntype EspnFootballEvent = {\n  date?: string;\n  competitions?: Array<{ competitors?: Array<{ homeAway?: string; team?: EspnFootballTeam }> }>;\n};\n\nfunction canonicalAliasList(team: EspnFootballTeam | undefined) {\n  const values = [team?.abbreviation, team?.displayName, team?.shortDisplayName, team?.location, team?.name]\n    .map((value) => textKey(value)).filter(Boolean);\n  return [...new Set(values)];\n}\n\nfunction canonicalTeamMatch(value: unknown, aliases: string[]) {\n  const key = textKey(value);\n  if (!key) return false;\n  const normalized = key\n    .replace(/^jac\\b/, \"jax\")\n    .replace(/^wsh\\b/, \"was\")\n    .replace(/^arz\\b/, \"ari\")\n    .replace(/^blt\\b/, \"bal\")\n    .replace(/^clv\\b/, \"cle\")\n    .replace(/^hst\\b/, \"hou\");\n  return aliases.some((alias) => {\n    const candidate = alias\n      .replace(/^jac\\b/, \"jax\")\n      .replace(/^wsh\\b/, \"was\")\n      .replace(/^arz\\b/, \"ari\")\n      .replace(/^blt\\b/, \"bal\")\n      .replace(/^clv\\b/, \"cle\")\n      .replace(/^hst\\b/, \"hou\");\n    return candidate === normalized || normalized.endsWith(\` \${candidate}\`) || candidate.endsWith(\` \${normalized}\`);\n  });\n}\n\nasync function loadCanonicalFootballGames(sport: FootballSport, dates: string[]) {\n  const games: CanonicalFootballGame[] = [];\n  const errors: string[] = [];\n  const league = sport === \"NFL\" ? \"nfl\" : \"college-football\";\n  for (const date of [...new Set(dates.filter(Boolean))].slice(0, sport === \"NFL\" ? 14 : 35)) {\n    try {\n      const url = new URL(\`https://site.api.espn.com/apis/site/v2/sports/football/\${league}/scoreboard\`);\n      url.searchParams.set(\"dates\", date.replace(/-/g, \"\"));\n      url.searchParams.set(\"limit\", sport === \"NFL\" ? \"100\" : \"500\");\n      if (sport === \"NCAAF\") url.searchParams.set(\"groups\", \"80\");\n      const response = await fetch(url, {\n        cache: \"no-store\",\n        headers: { \"User-Agent\": \"Mozilla/5.0 (compatible; EZPZ-Picks/1.0; +https://ezpzpicks.com)\" },\n        signal: AbortSignal.timeout(12_000),\n      });\n      if (!response.ok) throw new Error(\`ESPN schedule request failed \${response.status}\`);\n      const payload = await response.json() as { events?: EspnFootballEvent[] };\n      for (const event of payload.events || []) {\n        const competitors = event.competitions?.[0]?.competitors || [];\n        const away = competitors.find((item) => item.homeAway === \"away\")?.team;\n        const home = competitors.find((item) => item.homeAway === \"home\")?.team;\n        const awayAliases = canonicalAliasList(away);\n        const homeAliases = canonicalAliasList(home);\n        if (!awayAliases.length || !homeAliases.length) continue;\n        games.push({ date, awayAliases, homeAliases });\n      }\n    } catch (error) {\n      errors.push(\`schedule \${date}: \${error instanceof Error ? error.message : String(error)}\`);\n    }\n  }\n  return { games, errors };\n}\n\nfunction splitMatchesCanonicalGame(split: Split, game: CanonicalFootballGame) {\n  if (game.date !== split.date) return false;\n  if (!canonicalTeamMatch(split.awayTeam, game.awayAliases) || !canonicalTeamMatch(split.homeTeam, game.homeAliases)) return false;\n  if (split.market === \"Spread\") {\n    if (split.line == null || Math.abs(split.line) > 60) return false;\n    return canonicalTeamMatch(split.selectionTeam, game.awayAliases) || canonicalTeamMatch(split.selectionTeam, game.homeAliases);\n  }\n  if (split.side !== \"Over\" && split.side !== \"Under\") return false;\n  return split.line != null && split.line >= 20 && split.line <= 100;\n}\n\nfunction splitMatchesCanonicalSchedule(split: Split, games: CanonicalFootballGame[]) {\n  return games.some((game) => splitMatchesCanonicalGame(split, game));\n}\n\n`;
  if (!text.includes(anchor)) throw new Error("Could not find weekly market discovery anchor");
  text = text.replace(anchor, helper + anchor);
}

const returnBefore = "  return { splits: [...map.values()], errors };\n}";
const returnAfter = `  const discovered = [...map.values()];\n  if (!discovered.length) return { splits: [], errors };\n  const canonical = await loadCanonicalFootballGames(sport, discovered.map((split) => split.date));\n  errors.push(...canonical.errors);\n  const validated = discovered.filter((split) => splitMatchesCanonicalSchedule(split, canonical.games));\n  if (discovered.length !== validated.length) {\n    errors.push(\`Schedule/market validation rejected \${discovered.length - validated.length} non-\${sport} or malformed market sides.\`);\n  }\n  return { splits: validated, errors };\n}`;
if (text.includes(returnBefore)) text = text.replace(returnBefore, returnAfter);
else if (!text.includes("Schedule/market validation rejected")) throw new Error("Could not patch weekly market validation return");

fs.writeFileSync(path, text);
console.log("Weekly football markets now require a matching ESPN matchup plus a valid football spread/total selection before storage.");
